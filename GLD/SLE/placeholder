Because GitHub will do [folder jumping](https://github.blog/2014-08-19-folder-jumping/) if there is no other file inside a folder means we need to add a trivial file until we have a second SLE survey so that the top level GLD folder looks neater.
